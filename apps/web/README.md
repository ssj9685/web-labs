# Web Labs App

The deployed Web Labs frontend. It is kept under `apps/web` so future browser
feature labs can be added beside it or promoted into shared packages as needed.

```bash
npm run dev -w @web-labs/web
npm run build -w @web-labs/web
```
